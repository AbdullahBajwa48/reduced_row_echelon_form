this is a project which i made a library in python. you can install in your system using pip install rr_echeclon_f_48.
This uses row operations and there are separate functions for every row operation.
There is also a full finalized function.
